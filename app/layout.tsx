import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "IntegrateWise Docs",
  description: "Documentation for IntegrateWise — Knowledge Workspace powered by the Adaptive Spine and Continuity Bridge. Connect tools. Maintain truth and context. Let the Twin propose while you govern.",
  icons: {
    icon: "/logo.png",
  },
};

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
